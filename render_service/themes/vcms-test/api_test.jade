include config
doctype html
html
    head
        style.
            th, td {border: 1px solid #000; padding: 4px}
    body
        if data
            h1=data.title
        else
            h1 ERROR!!!11
        if simple
            h1=simple
        else
            h1(style="color: #f00")=simple_error