include config
doctype html
html
    head
        style.
            th, td {border: 1px solid #000; padding: 4px}
    body
        if get
            h1=get.title
            div="Parametrs is " + get.id
        else
            h1(style="color: #f00")=simple_error